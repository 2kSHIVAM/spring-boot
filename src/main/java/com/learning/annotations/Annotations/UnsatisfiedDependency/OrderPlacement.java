package com.learning.annotations.Annotations.UnsatisfiedDependency;

public class OrderPlacement {
    // note we could also use interface instead of using parent and child class, both will cause same error of unsatisfied dependency
}
